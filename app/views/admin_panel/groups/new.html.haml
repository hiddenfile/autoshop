=render :partial => 'head', :locals => {:title => 'Создание категории'}

= form_for @group, :url => admin_panel_groups_path, :html => {:multipart => true}  do |f|
  =render :partial => 'form', :locals => {:f => f}
  = f.submit 'Добавить'