.centerprod
  .pages Новинки интернет-магазина:
  %h6
  - @products.each do |product|
    .newprod_center
      .img
        = link_to (product.photos.first ? (image_tag product.photos.first.photo.url(:thumb)) : (image_tag ('/assets/noimage.png'))), company_group_product_path(product.company,product.group,product)
      .prodinfo
        %p #{product.title}
        %p Цена : #{product.price}
        %p= link_to_function "Добавить в корзину","addToCart('#{product.id}','1','#{product.title}','#{product.price}')"
.pagine
  %div
    = will_paginate @products
    .clear.centerprod
