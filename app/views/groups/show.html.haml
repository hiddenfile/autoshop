.centerprod
  - @group.companies.sort{|a,b| a.name<=>b.name}.each do |company|
    .img_center
      = link_to (image_tag company.photos.first.photo.url(:medium)), company_group_products_path(company,@group)
