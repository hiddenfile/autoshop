%h6.title
  =link_to @company.name,company_path(@company)
  >>
  =link_to @group.name,group_path(@group)
  >> Товары


%table#tablecontent.table
  %tr
    %th.first
    %th
    %th
    %th.last

  - @products.each do |product|
    %tr
      %td.offset=image_tag product.photos.first.photo.url(:thumb)
      %td.offset=link_to_function "Добавить в корзину","addToCartC('#{product.id}',1)"
      %td.offset=link_to product.title,company_group_product_path(@company,@group,product)
      %td=product.description
